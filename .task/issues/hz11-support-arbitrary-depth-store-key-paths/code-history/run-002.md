# Code Run run-002

- Mode: task-mode
- Task: 03-projections-and-phase
- Starting phase: coding
- Ending phase: coding
- Plan summary: Updated issue projections to materialize hierarchical document paths as markdown files, exposed logical document keys in issue inspection, and reverified phase-finalization behavior through the document command surface.
- Prior context used:
  - run-001: switched the in-progress `src/` CLI surface to `set`/`get`/`delete`, completed task `02-document-command-surface`, and left tasks `03-projections-and-phase` and `04-migration-and-import` open.
- What changed:
  - `src/tracker/stores.ts`, `src/tracker/projections.ts`, and `src/tracker/issues.ts` now materialize visible documents as `<path>.md`, read logical document keys from projections, and expose those keys to issue inspection.
  - `src/commands.ts`, `src/commands-registry.ts`, and `src/commands-shared.ts` now return `keys` from `show`, with `--include-keys` plus a deprecated `--include-stores` alias.
  - Updated command, projection, and phase tests to cover document-key issue inspection, markdown projection rebuilds, coexistence of a path as both document and subtree root, and phase-aware resaves through the document command surface.
- Verification:
  - `bun test`
  - `bun run lint`
  - `bun run typecheck`
  - `bun run /home/david/.pi/agent/skills/lint/scripts/lint.ts --diff`
  - observable CLI proof: create issue → `set research` + `set research/notes/today` → `show --include-keys` returns both keys → `.task/issues/<id>/research.md` and `.task/issues/<id>/research/notes/today.md` contain the saved content
- Commit: 3c60a8f54d33f04d7038036c7220fe9e9766ef3f
- Remaining work / open problems:
  - task `04-migration-and-import` remains open
- Next recommended command: /skill:code hz11
