---
run_key: run-007
issue_id: hz11
timestamp_utc: 2026-04-21T17:29:53Z
mode: task-mode
active_task: 08-root-cli-surface-parity
outcome: success
next_command: /skill:code hz11 09-supported-docs-parity
previous_runs:
  - run-001
  - run-002
  - run-003
  - run-004
  - run-005
  - run-006
starting_signals:
  - research:plan
  - tasks:08-root-cli-surface-parity
  - code-history:latest=run-006
ending_signals:
  - task-status:08-root-cli-surface-parity=done
  - code-history:latest=run-007
---

# Code Run run-007

## Plan Summary
Aligned the repo-root `bun task.ts` entrypoint with the approved path-based document command surface by reusing the staged CLI registry/help behavior, then added root-entrypoint regressions for help, document dispatch, and invalid-path failures.

## Prior Context Used
- run-001 — switched the staged `src/` CLI surface to `set`/`get`/`delete` and removed staged `store` help.
- run-002 — materialized path-based document projections and issue key inspection.
- run-003 — preserved legacy history/import compatibility under the path-based document model.
- run-004 — repaired the nullable-props lint finding in legacy-history tests.
- run-005 — decomposed oversized legacy-history tests to satisfy function-size lint.
- run-006 — reran the full automated gates after the earlier repair batch.

## What Changed
- task.ts — replaced the legacy root command registry/help/flag handling with the shared `src/commands-registry`-backed CLI flow so `bun task.ts` now exposes the approved `set`/`get`/`delete` document surface and uses the same path validation/dispatch behavior as the staged entrypoint.
- task.test.ts — added root CLI regressions for help output without `store` commands, `set --help`, successful document command dispatch through `bun task.ts`, and clear invalid-path errors for rejected exact selectors.

## Verification
- [pass] bun test task.test.ts — root CLI regression coverage passed, including the new help, dispatch, and invalid-path cases.
- [pass] bun run lint — project lint passed with zero warnings/errors.
- [pass] bun run /home/david/.pi/agent/skills/lint/scripts/lint.ts --diff — custom diff lint reported no findings.
- [pass] bun run typecheck — full TypeScript typecheck passed.
- [pass] bun task.ts --help — root help now advertises `set`, `get`, and `delete` and no longer shows `store set|get|keys|delete`.
- [pass] observable invalid-path proof — `bun task.ts set <id> --key research/ --value hello` exits 1 with `Subtree selector 'research/' is not allowed here`.

## Commits
- 7ab546ed663e37c6e2eab3d994e4179e10851631 — align the repo-root `task.ts` entrypoint with the approved document command surface and add root CLI parity regressions.

## Remaining Work / Open Problems
- task 09-supported-docs-parity remains open.
